{
  "name": "mailzen",
  "version": "1.0.0",
  "description": "MailZen - Email management platform",
  "private": true,
  "workspaces": [
    "apps/*",
    "libs/*"
  ],
  "scripts": {
    "start": "nx run-many -t serve -p frontend backend --parallel=2",
    "dev": "nx run-many -t serve -p frontend backend --parallel=2",
    "dev:all": "nx run-many -t serve -p marketing frontend backend --parallel=3",
    "dev:full": "nx run-many -t serve -p marketing frontend backend ai-agent-platform --parallel=4",
    "dev:platform": "nx run-many -t serve -p backend ai-agent-platform --parallel=2",
    "dev:ai-agent": "nx serve ai-agent-platform",
    "start:ai-agent:prod": "nx run ai-agent-platform:serve:prod",
    "install:all": "echo \"Deprecated: use 'npm install' at repo root (npm workspaces)\" && exit 1",
    "dev:frontend": "nx serve frontend",
    "dev:backend": "nx serve backend",
    "dev:marketing": "nx serve marketing",
    "build": "nx run-many -t build -p frontend backend",
    "build:frontend": "nx build frontend",
    "build:backend": "nx build backend",
    "build:marketing": "nx build marketing",
    "build:all": "nx run-many -t build -p marketing frontend backend --parallel=3",
    "lint": "nx run-many -t lint -p frontend backend --parallel=2",
    "lint:marketing": "nx lint marketing",
    "lint:ai-agent": "nx lint ai-agent-platform",
    "test:ai-agent": "nx test ai-agent-platform",
    "format": "nx run-many -t format -p frontend backend --parallel=2",
    "format:check": "nx run-many -t format:check -p frontend backend --parallel=2"
  },
  "keywords": [
    "email",
    "management",
    "nextjs",
    "nestjs"
  ],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "nx": "^20.3.1"
  },
  "dependencies": {
    "recharts": "^3.7.0"
  }
}
